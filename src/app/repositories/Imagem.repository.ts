export default class ImagemRepository {
	static async criarImagem(finalidade: string) {}
	static async confirmarImagem(
		finalidade: string,
		id_finalidade: string,
		caminho: string
	) {}

	static async removerImagem(
		finalidade: string,
		id_finalidade: string,
		caminho: string
	) {}
}
